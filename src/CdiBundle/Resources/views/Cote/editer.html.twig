{% extends '::base.html.twig' %}

{% block stylesheets %}
    <style>
        table {
            margin-top: 20px;
        }
    </style>
{% endblock %}

{% block body %}
    <h2 class="fil_ariane"><a href="{{ path('administration') }}">Administration</a> > <a href="{{ path('cote_liste') }}">Gestion des cotes</a> > {{ 'édition d\'une cote'|upper }}</h2>
    <section class="section">
        <h4 style="margin: 0; text-transform: uppercase;">Édition de la cote :</h4>
        {{ form_start(form) }}
            {{ form_errors(form) }}
            <table>
                <tr>
                    <td>
                        {# Nom de la cote #}
                        {{ form_label(form.nom, "Nom de la cote :") }}
                    </td>
                    <td>
                        {{ form_widget(form.nom, {'attr': {'placeholder': 'Nom'}}) }}
                    </td>
                </tr>
                <tr>
                    <td>
                        <br/>
                        {# Submit button #}
                        {{ form_widget(form.ajout, {'label': 'Modifier la cote'}) }}
                    </td>
                </tr>
            </table>

            {{ form_rest(form) }}
        {{ form_end(form) }}
    </section>
{% endblock %}
